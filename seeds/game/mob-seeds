const {Mob} = require("../../models");

const mobData = [
    {
        EnemyId: 1,
        StoryId: 1
    },
    {
        EnemyId: 2,
        StoryId: 6
    }
]

const seedMob = () => Mob.bulkCreate(mobData);

module.exports = seedMob;